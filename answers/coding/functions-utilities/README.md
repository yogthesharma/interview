# Coding — functions & utilities

**15 problems** — bare stubs + HackerRank-style tests.

```bash
cd answers/coding/functions-utilities
node 06-memoize.js
node 01-debounce.js   # async — takes ~1s
```

| File | Problem |
|------|---------|
| `01` | Debounce |
| `02` | Throttle |
| `03` | Promise.all from scratch |
| `04` | Retry with exponential backoff |
| `05` | Simple rate limiter |
| `06` | Memoization |
| `07` | LRU cache |
| `08` | once |
| `09` | compose / pipe |
| `10` | Event emitter (pub/sub) |
| `11` | bind from scratch |
| `12` | Curry |
| `13` | Serialize concurrent calls (queue) |
| `14` | Promise timeout wrapper |
| `15` | sleep / delay |

**Also prepped elsewhere:** debounce/throttle (`javascript/09`), retry & Promise.all (`javascript-async/`). These files are **coding-style** reps with automated tests.

**Solutions:** `solutions/` — peek only if stuck.
