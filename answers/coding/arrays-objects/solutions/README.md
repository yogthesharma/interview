# Solutions — peek only if stuck

Copy approach into the matching `../*.js` file, or run to see passing output:

```bash
node solutions/01-reverse-without-mutate.js
```

Try for 15+ min first — interview value is in your attempt.
