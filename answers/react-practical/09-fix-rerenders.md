# 09 — Fix excessive re-renders

**Route:** `/09-fix-rerenders` · **File:** `Page09FixRerenders.tsx`

**Fix** buggy code — `React.memo` + stable `useCallback`. Typing should not rerender child every key.

See `solutions/Page09FixRerenders.tsx`.
